# future_kb/
This folder is intentionally scaffolded because law_articles and tx_law_map are not produced yet.
Planned scripts:
- ingest_law_sources.py  (parse 'المادة' into law_articles + rebuild law_fts)
- generate_tx_law_map.py (first-pass mapping tx -> law articles)
- mapping_csv.py         (export/import review CSV for human-in-the-loop)
- kb_diagnostics.py      (coverage + join sanity)
