# Feature Controls

Feature local edit, dirty state, warning `Discard` restoration, and broad-action confirmation with `Cancel` passed in the authenticated UI. Request observation found zero `PUT` requests whose pathname ends with `/api/admin/features`; `Save & Publish` was not pressed.
