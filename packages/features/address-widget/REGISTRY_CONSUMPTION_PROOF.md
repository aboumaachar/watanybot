# Registry Consumption Proof

The address-widget plugin now contains a proof-only TypeScript module that imports the host registration and returns a typed proof object.

Required validation:
- pnpm -s typecheck
- pnpm -s lint
- optional browser/API smoke when servers are running