# Runtime releases

Only an approved immutable release may be placed here. A release requires canonical JSON, alias JSON, manifest, hashes, statistics, validation report, version, and approval metadata.

Current state: no release installed; runtime loading is intentionally blocked.
