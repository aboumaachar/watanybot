# Schemas

The TypeScript contract in `src/types.ts` is the executable schema for the initial platform slice. A JSON Schema export will be generated only when the approved release shape is finalized.
