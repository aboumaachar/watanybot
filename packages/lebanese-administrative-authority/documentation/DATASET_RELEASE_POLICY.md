# Dataset Release Policy

A release is consumable only when its manifest is approved, releasedAt is present, at least one approver is named, canonical and alias hashes match exact runtime bytes, and validation reports production readiness.

No release is published when coordinates, provenance, Arabic names, hierarchy, or consumer migration evidence is incomplete. Runtime failure is the required behavior in that state.
