# Validation

Validation is fail-closed for duplicate IDs, orphan hierarchy rows, missing Arabic names, missing provenance, hash mismatches, and unapproved release metadata. Missing coordinates keeps a structurally valid candidate out of production readiness.
