# Alias engine inputs

Alias files belong to an approved release and must map each active alias to exactly one canonical locality ID. Ambiguous aliases remain unresolved and must not be silently assigned.
